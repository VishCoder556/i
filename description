NEW FRANCUA LINGUA LANGUAGE

.i -- I programming language
    This programming language is inspired from the term "I", which means a language that unites many people of different ethnic backgrounds. The programming equivalent, as you would expect, derives from this term in computer science. Basically, it is a common programming language that unites people who code differently (C developers vs Python developers, for an example)




int a = 5
int b = 10
exit(a + b)

------------------
// In C, you would need to map the top-level declarations into a `main` function. The following also applies for Rust and many other languages
#include <stdio.h>

int main(){
    int a = 5;
    int b = 10;
    return a + b;
}


------------------

// You might have to include multiple files for the Rust backend
use std:process;
fn main(){
    let a : i32 = 5;
    let b : i32 = 10;
    process::exit(a + b);
}

------------------
# types are ignored in Python mode
a = 5
b = 10
exit(a + b)
